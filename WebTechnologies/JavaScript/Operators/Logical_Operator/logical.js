// Logical and (&&) 
let a=70;
let b=50;
let c=90;
var firstrank=(a>=50 && b>=50 && c>=50)
console.log(firstrank," You are in first class");
var secondclass=(a<50 && b<=50 && c<=50)
console.log(secondclass,"You are in second class");

// Logical or (||) 
let unitopper=200;
let clgtopper=180;
var topper=(unitopper>=190 || clgtopper>=190)
console.log(topper,"You are eligible for gold medal");

// Logical not (!) 
let x=50;
let y=39;
console.log(!(a>b));//false
console.log(!(a<b));//true
